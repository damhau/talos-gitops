{{- range $path, $_ := .Files.Glob "dashboards/*.json" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-{{ $path | base | trimSuffix ".json" }}
  labels:
    grafana_dashboard: "1"
data:
  {{ $path | base }}: |-
    {{ $.Files.Get $path | nindent 4 }}
---
{{- end }}
